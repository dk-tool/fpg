apiVersion: v1
kind: Service
metadata:
  name: ngc-pod2
  namespace: openshift-gitops
spec:
  type: ClusterIP
  selector:
    app: ngc-pod2
  ports:
    - name: jupyter
      protocol: TCP
      port: 80
      targetPort: 8888
